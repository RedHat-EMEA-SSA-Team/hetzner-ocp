# {{ ansible_managed }}
# cloud-config
ssh_pwauth: true
ssh_authorized_keys:
- {{ lookup('file', '~/.ssh/id_rsa.pub') }}

chpasswd:
  list: |
     root:{{default_password}}
     cloud-user:{{default_password}}
  expire: False

timezone: Europe/Helsinki
